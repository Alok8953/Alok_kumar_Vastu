{
  "name": "@app/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch src/server.js",
    "start": "node --env-file=.env src/server.js",
    "db:init": "node -e \"import('./src/db/initDb.js').then(m => m.initDatabase()).then(() => console.log('Database ready')).catch(e => { console.error(e.message); process.exit(1); })\""
  },
  "dependencies": {
    "@repo/shared-types": "1.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.1",
    "nodemailer": "^8.0.7",
    "pg": "^8.21.0",
    "resend": "^6.18.0"
  }
}
